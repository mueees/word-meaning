seed-blade
  display block

  .content
    background main-second-color
    color white
    position fixed
    top 0
    bottom 0
    width 80%
    transition transform 0.3s
    z-index 20

    &.left
      &.expanded
        transform translateX(0);
      &.collapsed
        transform translateX(-100%);